# Copy to ~/.config/cursor-outpost/.env — never commit the real .env file.
# This repo only tracks this empty template.

CURSOR_API_KEY=
TELEGRAM_BOT_TOKEN=
# Comma-separated Telegram numeric user ids allowed to use the bot
TELEGRAM_ALLOW_USER_IDS=
